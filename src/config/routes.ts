interface Routes {
  blog: string
  login: string
  home: string
  register: string
}

const routes: Routes = {
  blog: '/blog',
  login: '/login',
  home: '/',
  register: '/register'
}

export default routes
